{
  "name": "la-cite-des-voleurs-back",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "commonjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "rm -r dist && npx tsc && cp -r src/assets dist && pm2 start dist/app.js",
    "dev": "rm -r dist && npx tsc && cp -r src/assets dist && node dist/app.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "pm2": "^5.3.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.1",
    "typescript": "^5.4.3"
  }
}